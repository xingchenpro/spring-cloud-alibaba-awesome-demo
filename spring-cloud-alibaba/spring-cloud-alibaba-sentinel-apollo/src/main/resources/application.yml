spring:
  application:
    name: sentinel
  cloud:
#    nacos:
#      discovery:
#        server-addr: localhost:8848
    sentinel:
      transport:
        dashboard: localhost:8718
      eager: true
      datasource:
        ds:
          apollo:
            namespace-name: application
            flow-rules-key: flowRules
            rule-type: flow
server:
  port: 8889
  tomcat:
    max-threads: 20

app:
  id: sentinel

apollo:
  meta: http://49.233.192.8:8080
  cacheDir: /app/war/apollo/temp/